---
title: "CraftCommand: Annotation-Driven Java Command Framework"
sidebarTitle: "Home"
description: "CraftCommand generates reflection-free command wrappers at compile time using Java annotation processing. Supports Standalone, Bukkit/Spigot, and Paper (Brigadier)."
---

CraftCommand is an opinionated, annotation-driven command framework for Java. Instead of scanning annotations at runtime with reflection, CraftCommand's annotation processor reads your `@Command` and `@Subcommand` declarations at **compile time** and generates platform-native wrapper classes using [JavaPoet](https://github.com/palantir/javapoet). The result is near-native dispatch speed with no per-invocation reflection overhead.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Build and register your first annotated command in minutes
  </Card>
  <Card title="Installation" icon="box" href="/installation">
    Add CraftCommand modules to your Maven project with the BOM
  </Card>
  <Card title="Core Concepts" icon="lightbulb" href="/concepts/annotation-processing">
    Understand how compile-time processing and code generation work
  </Card>
  <Card title="Annotation Reference" icon="at" href="/reference/annotations-core">
    Complete reference for every annotation CraftCommand provides
  </Card>
</CardGroup>

## Why CraftCommand?

Most Java command frameworks rely on runtime reflection to discover and invoke annotated methods. CraftCommand takes a different approach: **the annotation processor runs during `javac`** and emits ordinary Java wrapper classes. By the time your application starts, the command routing is just a chain of plain method calls.

<CardGroup cols={3}>
  <Card title="Zero Reflection per Call" icon="bolt">
    One `Class.forName` at registration time; zero reflection during every subsequent execution
  </Card>
  <Card title="Multi-Platform" icon="layer-group">
    Standalone Java, Bukkit/Spigot, and Paper (Brigadier) — one annotation model across all platforms
  </Card>
  <Card title="Extensible via SPI" icon="plug">
    Register custom argument resolvers, validation handlers, and method handlers through standard Java SPI
  </Card>
</CardGroup>

## Platform Support

<Tabs>
  <Tab title="Standalone">
    Use CraftCommand in any Java application — CLI tools, Discord bots, chatbots, or test harnesses. No Minecraft dependency required.

    ```xml pom.xml
    <dependency>
        <groupId>io.github.projectunified</groupId>
        <artifactId>craftcommand-standalone-runtime</artifactId>
    </dependency>
    ```
  </Tab>
  <Tab title="Bukkit / Spigot">
    Register commands directly into Bukkit's `CommandMap` with built-in `Player`, `World`, and `Location` type resolvers.

    ```xml pom.xml
    <dependency>
        <groupId>io.github.projectunified</groupId>
        <artifactId>craftcommand-bukkit-runtime</artifactId>
    </dependency>
    ```
  </Tab>
  <Tab title="Paper (Brigadier)">
    First-class support for Paper's Brigadier command API. The processor generates Brigadier `LiteralCommandNode` trees for rich client-side tab completion.

    ```xml pom.xml
    <dependency>
        <groupId>io.github.projectunified</groupId>
        <artifactId>craftcommand-paper-runtime</artifactId>
    </dependency>
    ```
  </Tab>
</Tabs>

## Get Started in 4 Steps

<Steps>
  <Step title="Add the BOM and dependencies">
    Import `craftcommand-bom` in your `<dependencyManagement>` block, then declare the annotations and runtime artifacts for your target platform.
  </Step>
  <Step title="Configure the annotation processor">
    Add the platform-specific processor artifact (e.g. `craftcommand-standalone-processor`) to `maven-compiler-plugin`'s `<annotationProcessorPaths>`. The processor runs automatically during `mvn compile`.
  </Step>
  <Step title="Write your command class">
    Annotate a class with `@Command`, define subcommands with `@Subcommand`, mark the default handler with `@Default`, and declare optional parameters with `@Optional`.
  </Step>
  <Step title="Register and execute">
    Instantiate the platform's `CommandManager`, call `manager.register(new YourCommand())`, and dispatch user input with `command.execute(sender, args)`.
  </Step>
</Steps>

<Note>
  CraftCommand targets Java 8 and above. All generated wrapper classes are plain Java — no bytecode manipulation, no proxies.
</Note>
